# Codex Manager 未发布变更

> `v0.4.0` 定版后，新的用户可见变更继续记录在这里；发布时移入新的 `v<VERSION>.md`，不得改写已有 tag 的版本事实。

## 概览

暂无已分配版本的用户可见变更。

## 主要变化

- 后续用户可见变更将在下一次定版时补充。

## 验证状态

- `implemented`：暂无未分配版本的变更。
- `tested`：无未分配版本的新增测试。

## 兼容性与限制

以最近已定版的 release note 和 `docs/release.md` 为准。

## 隐私与安全

未分配版本的变更不引入新的隐私或安全行为。

## 发布状态

`published`：不适用。`observed`：无未分配版本的变更。`accepted`：不适用。`cleanup`：无待清理发布资源。

## 完整性

尚无下一版本 tag、exact source SHA 或发布资产。
