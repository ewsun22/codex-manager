# Codex Manager 未发布变更

> `v0.6.1` 定版后，新的用户可见变更继续记录在这里；不得改写已有 tag 的版本事实。

## 概览

当前没有新的未分配版本变更。

## 主要变化

- `implemented`：暂无新增实现；`v0.6.1` 功能事实已移入 `docs/release-notes/v0.6.1.md`。

## 验证状态

- `tested`：暂无未发布切片测试；不得由 `v0.6.1` 的验证结果外推。
- `observed`：暂无新的未发布切片观测证据。

## 兼容性与限制

暂无新的兼容性变化；当前边界以最近版本化 note 为准。

## 隐私与安全

暂无新的隐私或安全变化；API Key、token、bearer 和消息正文仍不得进入普通 DTO、SQLite 或日志。

## 发布状态

- `published`：否；没有新的未发布版本。
- `accepted`：否；暂无新的验收范围。
- `cleanup`：暂无新增运行资源或临时发布状态。

## 完整性

没有新的未分配版本号、tag、exact source SHA 或发布资产。
