# Codex Manager 未发布变更

> `v0.3.0` 定版后，新的用户可见变更继续记录在这里；发布时移入新的 `v<VERSION>.md`，不得改写已有 tag 的版本事实。

## 概览

当前没有已实现但尚未分配版本的用户可见变更。

## 主要变化

- 暂无；后续功能必须在实现提交中同步补充适用文档和本页。

## 验证状态

- `implemented`：暂无新的未发布功能。
- `tested`：暂无新的未发布功能需要验证。

## 兼容性与限制

以最近已定版的 release note 和 `docs/release.md` 为准。

## 隐私与安全

未新增网络、OAuth、WebView 或文件系统权限。

## 发布状态

`published`：不适用。`observed`：不适用。`accepted`：不适用。`cleanup`：无待清理发布资源。

## 完整性

尚无新 tag、exact source SHA 或发布资产。
